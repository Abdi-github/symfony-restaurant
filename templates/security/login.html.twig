{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block navbar %}{% endblock %}

{% block content %}

{# 
<div class="d-flex justify-content-center align-items-center vh-100 "  >
    <div class="card p-2 bg-dark border-warning">
      <h2 class="card-header text-center text-uppercase text-warning ">Sign In</h2>
      <div class="card-body">
    
        <form method="post">
            {% if error %}
                <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}
            {% if app.user %}
                <div class="mb-3 text-warning">
                    You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a>
                </div>
            {% endif %}
            <div class="mb-3  ">
                <input class="form-control " type="email" name="email" id="inputEmail" value="{{ last_username }}" autocomplete="email" required autofocus placeholder="Email" aria-label=".form-control-lg example">
            </div>
            <div class="mb-3 ">
                <input class="form-control" type="password" name="password" id="inputPassword"
                value="{{ last_username }}" autocomplete="current-password" required autofocus placeholder="Password" aria-label=".form-control-lg example">
            </div>
    
            <input type="hidden" name="_csrf_token"
                   value="{{ csrf_token('authenticate') }}"
            >
    
         
                <button class="btn btn-warning mb-3" type="submit">
                    Sign in
                </button>

                <div><a class="text-warning fst-italic fs-6"  href="{{path('app_forgot_password_request')}}">Forgot password?</a></div>
          
        </form>
    
      </div>
    </div>
</div>

#}
<div class="container d-flex flex-column vh-100 justify-content-center" >
<div class="col-md-6 mx-auto ">
 <div class="card bg-dark p-2">
  <h2 class="card-header text-center text-uppercase text-warning  ">Sign In</h2>
     <div class="card-body mt-4">
         <form method="post">
                    {% if error %}
                        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                    {% endif %}
                    {% if app.user %}
                        <div class="mb-3 text-warning">
                            You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a>
                        </div>
                    {% endif %}
                    <div class="mb-4 p-2">
                        <input class="form-control border-2 border-warning " type="email" name="email" id="inputEmail" value="{{ last_username }}" autocomplete="email" required autofocus placeholder="Email" aria-label=".form-control-lg example">
                    </div>
                    <div class="mb-4 p-2 ">
                        <input class="form-control  border-2 border-warning" type="password" name="password" id="inputPassword"
                        value="{{ last_username }}" autocomplete="current-password" required autofocus placeholder="Password" aria-label=".form-control-lg example">
                    </div>
         
                    <input type="hidden" name="_csrf_token"
                           value="{{ csrf_token('authenticate') }}"
                    >
         
         
                        <div class="d-flex justify-content-center mt-4 ">
                            <button class="btn btn-warning mb-3 w-50" type="submit">
                                Sign in
                            </button>
                        </div>
                        <div class="d-flex justify-content-between mt-4">
                            <a class="text-warning fst-italic fs-6"  href="{{path('app_forgot_password_request')}}">Forgot password?</a>
                            <a class="text-warning fst-italic fs-6"  href="{{path('home')}}">Go To Homepage</a>
                        </div>
                        
         
                </form>
     </div>
 </div>
</div>
</div>
   

{% endblock %}
